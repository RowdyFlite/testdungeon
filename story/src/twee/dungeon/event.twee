:: Event
<<set _evt=$current_room.event>>

<<if _evt.type is "battle">>\
	<<link "Combat!" "Combat">>
		<<startCombat _evt.id>>
	<</link>>
<<else>>\
	Couldn't find switch for event type: _evt.type
<<endif>>\